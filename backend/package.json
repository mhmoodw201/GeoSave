{
  "name": "geosave-backend",
  "version": "2.0.0",
  "private": true,
  "description": "GeoSave — location-aware rental marketplace API and web server",
  "main": "server.js",
  "engines": {
    "node": ">=20.9"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test --test-reporter=spec tests/*.test.js",
    "migrate": "node scripts/migrate.js",
    "make-admin": "node scripts/make-admin.js",
    "build:demo": "node scripts/build-demo.js"
  },
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^18.0.3",
    "express": "^4.22.3",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.24.4",
    "multer": "^2.4.0",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "mongodb-memory-server": "^11.3.0",
    "supertest": "^7.3.0"
  }
}
